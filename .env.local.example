# Admin credentials — REQUIRED. Generate with: node scripts/hash-password.js "yourPassword"
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=
# OR (development only — will be auto-hashed at runtime):
# ADMIN_PASSWORD=changeme

# JWT signing secret — at least 32 characters of high entropy
ADMIN_JWT_SECRET=
